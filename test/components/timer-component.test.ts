import { equal, notEqual, notDeepStrictEqual } from 'assert';
// import { Mock } from 'typemoq';

suite('timer component tests else', () => {
    test("Something 1", () => {
        equal(-1, [1, 2, 3].indexOf(5));
        equal(-1, [1, 2, 3].indexOf(0));
    });
});



