import { assert } from 'chai';
import { getTasks, createStore } from '../../src/services/task-store';
import { activate } from '../../src/extension';


suite('task store tests', () => {
  test('getTasks should return an empty array if no tasks exits', () => {
  });
})
